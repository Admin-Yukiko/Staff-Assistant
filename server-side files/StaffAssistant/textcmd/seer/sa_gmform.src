use uo;

program gm(who, help)

if (help)
        SendSysMessage(who, "This command gives you the GM form. Use .myform to switch back");
else
		SetObjProperty(who, "pre_myform_graphic", who.graphic);
        who.graphic := 0x3db;
endif
endprogram